{
  "name": "text-magnifier",
  "version": "1.0.0",
  "description": "Chrome extension that magnifies text on hover with smooth animation",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "tsc",
    "prebuild": "npm run clean && npm run compile",
    "build": "cp manifest.json content.css dist/ && cp -r icons dist/",
    "postbuild": "cd dist && zip -r text-magnifier.zip manifest.json content.js content.css icons/",
    "clean": "rm -rf dist",
    "dev": "tsc --watch"
  },
  "keywords": [
    "chrome-extension",
    "accessibility",
    "text-magnifier"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.1.33",
    "typescript": "^5.9.3"
  }
}
