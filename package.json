{
  "name": "text-magnifier",
  "version": "1.0.0",
  "description": "Chrome extension that magnifies text on hover with smooth animation",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "tsc",
    "prebuild": "npm run clean && npm run compile",
    "build": "cp manifest.json content.css src/popup.html dist/ && cp -r icons dist/",
    "postbuild": "cd dist && zip -r text-magnifier.zip manifest.json content.js content.css popup.html popup.js icons/",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "npm run build && playwright test --grep-invert '手動操作'",
    "test:headed": "npm run build && playwright test --headed --grep-invert '手動操作'",
    "test:manual": "npm run build && playwright test --headed --grep '手動操作'"
  },
  "keywords": [
    "chrome-extension",
    "accessibility",
    "text-magnifier"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/chrome": "^0.1.33",
    "serve": "^14.2.5",
    "typescript": "^5.9.3"
  }
}
